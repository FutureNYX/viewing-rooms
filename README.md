Static host for ROBINEAU viewing rooms.

Contents are AES-256-GCM ciphertext. The decryption key is never stored here:
it travels in the URL fragment, which browsers do not send to the server.
Without the full link these files are unreadable.
